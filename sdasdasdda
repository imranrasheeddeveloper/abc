struct ContentView: View {
    var body: some View {
        VStack(spacing: 20) {
            ReassemblableButton(variant: .primary)
            ReassemblableButton(variant: .secondary)
            ReassemblableButton(variant: .tertiary)
        }
        .padding()
        .background(Color(UIColor.systemBackground))
    }
}
